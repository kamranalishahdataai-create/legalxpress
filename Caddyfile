{
	# Railway terminates TLS at its edge and routes to a fixed internal port.
	admin off
	auto_https off
}
:8080 {
	root * /usr/share/caddy
	encode gzip
	try_files {path} /index.html
	file_server
	header /assets/* Cache-Control "public, max-age=2592000, immutable"
}
